#include <stdio.h>

/**
 * main - prints all lowercase letters except q and e
 * Return: 0 if executed successfully
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
